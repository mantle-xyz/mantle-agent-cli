# Concepts Overview

This section explains the high-level model behind Mantle Agent Scaffold in v0.1.0.

## What Mantle Agent Scaffold Is

Mantle Agent Scaffold is a **CLI and core library** that exposes Mantle L2 read and analysis capabilities to AI agents.

The system is built around a strict operational boundary:

- tools provide structured, deterministic outputs
- agents orchestrate tool usage from natural language
- security and correctness are enforced at the tool layer

## Design Priorities

### 1. Read-First Safety

The current scope emphasizes read-only and analysis workflows. Even when transaction-oriented capabilities are discussed in roadmap context, safety rules and validation gates are treated as first-class constraints.

### 2. Predictable Contracts

Each tool has explicit input schema, stable output shape, and typed error semantics. This allows tests to catch regressions in tool behavior.

### 3. Agent Usability

Descriptions, schemas, and tool outputs are written to maximize correct tool routing by LLMs.

## Key Conceptual Layers

1. **CLI Layer**: command entry point for chain reads, DeFi queries, and transaction building.
2. **Domain Tool Layer**: chain, token, account, DeFi read, indexer, diagnostics.
3. **Policy Layer**: endpoint allowlists, input validation, read-only SQL restrictions.
4. **Evaluation Layer**: unit tests plus integration tests.

## Recommended Next Read

- [Skills Usage](/concepts/skills)
- [External Agent Integration](/concepts/external-agents)
