{
  "name": "mantle-agent-scaffold",
  "private": true,
  "version": "0.1.18",
  "license": "MIT",
  "description": "Monorepo for Mantle L2 tooling — core library and CLI",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/cli",
    "typecheck": "npm run typecheck -w packages/core && npm run typecheck -w packages/cli",
    "pretest": "npm run build",
    "test": "npx vitest run",
    "skills:init": "git submodule update --init --recursive skills",
    "skills:sync": "git submodule update --init --remote skills && git add skills",
    "docs:dev": "npm --prefix docs run dev",
    "docs:build": "HOME=$PWD/docs npm --prefix docs run build"
  },
  "devDependencies": {
    "@types/node": "^20.17.58",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
